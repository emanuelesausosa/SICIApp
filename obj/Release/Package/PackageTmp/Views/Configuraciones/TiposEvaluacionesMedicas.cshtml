@model IEnumerable<SICIApp.Models.TIPOEVALUACIONMEDICAMODEL>

@{
    ViewBag.Title = "Tipos Evaluaciones Médicas";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-sm-4">
        <h2>Tipos Evaluaciones Médicas</h2>
        <ol class="breadcrumb">
            <li>
                @Html.ActionLink("Configuraciones","Index")
            </li>
            <li class="active">
                <strong>TIPO EVALUACIÓN MEDICA</strong>
            </li>
        </ol>
    </div>
</div>


<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5>Lista de TIPO EVALUACION MÉDICA</h5>
					<div class="ibox-tools">
						@Html.ActionLink("Crear Nueva", "NuevoTipoEvaluacionMedica", null, new { @class = "btn btn-primary btn-xs"})
					</div>
                </div>
                <div class="ibox-content">

<table class="table table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CATEGORIA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DESCRIPCION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CATEGORIA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPCION)
        </td>
        <td>
			@Html.ActionLink("Detalles", "DetalleTipoEvaluacionMedica", new { ID=item.ID }, new { @class = "btn btn-primary btn-sm"})
            @Html.ActionLink("Editar", "EditarTipoEvaluacionMedica", new { ID=item.ID }, new { @class = "btn btn-white btn-sm"})            
        </td>
    </tr>
}

</table>

                </div>
            </div>
        </div>
    </div>
 </div>



